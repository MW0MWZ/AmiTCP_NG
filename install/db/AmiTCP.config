# AmiTCP_NG stack configuration (AmiTCP:db/AmiTCP.config).
# The loopback interface (127.0.0.1) is brought up automatically at startup and
# local traffic always uses it -- there is no longer a useloopback knob.
